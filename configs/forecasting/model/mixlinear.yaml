# @package model
# Learned forecaster — set ``model.release_dir`` or
# ``model.mixlinear.checkpoint_path`` to a trained checkpoint.
type: mixlinear
name: null
release_dir: null
mixlinear:
  checkpoint_path: ???
  device: auto
  batch_size: 64
  period_len: 24
  lpf: 2
  alpha: 0.5
  rank: 2
