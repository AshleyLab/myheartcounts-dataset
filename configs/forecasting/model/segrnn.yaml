# @package model
# Learned forecaster — set ``model.segrnn.checkpoint_path`` to a trained ckpt.
type: segrnn
segrnn:
  checkpoint_path: ???
  device: auto
  batch_size: 64
  seg_len: 24
  d_model: 64
  dropout: 0.1
