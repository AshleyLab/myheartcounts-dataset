# @package method
type: xgboost
# Build-on-miss feature cache dir (null = the default; full-history cache for
# max_future_days=0, see XGBoost._resolve_features_dir).
features_dir: null
# 0 = no temporal constraint = full history (the validated benchmark default).
# A positive value selects the forward-windowed ablation.
max_future_days: 0
