# @package evaluation
# Which splits to evaluate. Default ["val", "test"] preserves historical
# behavior. For leaderboard-only reruns (paper pipeline reads --splits test
# exclusively) pass ["test"] to skip val.
eval_splits: ["val", "test"]
