# @package training
epochs: 100
batch_size: 32
patience: 10
optimizer_lr: 0.001
weight_decay: 0.0
clip_grad_norm: null         # set to e.g. 1.0 to enable gradient clipping
device: auto
num_workers: 4
