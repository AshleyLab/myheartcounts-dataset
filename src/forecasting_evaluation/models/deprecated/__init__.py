"""Deprecated forecasting models.

Models here are **not** part of the active Track-3 leaderboard and are not wired
into the model registry (``models/registry.py``), the config schema
(``config.py``), or the Hydra type registry (``hydra/registry.py``). They are
kept (importable) only for reference / reproducing prior runs.

See each module's docstring for why it was deprecated and what replaces it.
"""
