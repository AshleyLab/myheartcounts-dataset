"""Data layer for the prediction track.

``provider`` derives per-task cohorts/labels from the labels lookup;
``loader`` materializes each participant's eligible raw segments;
``splits`` reads the frozen user splits.
"""
